import React from 'react'

function NavBar() {
    return( 
        <nav className="nav-bar">
            <div className="logo">
              <span role="img">🪙</span>
              <h1>CRYPTOBOARD</h1>
            </div>
        </nav>
        );
}

export default NavBar